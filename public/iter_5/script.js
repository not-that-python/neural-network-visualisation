import * as func from './functions/funcs.js'
import * as tf from '@tensorflow/tfjs'

// get relevant elements from the DOM
let ctx = canvas.getContext('2d')

// testing drawing to the canvas
// ctx.fillStyle = 'red'
// ctx.fillRect(0, 0, 120, 120)